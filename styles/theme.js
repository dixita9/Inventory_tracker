import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#604CC3', // Customize the primary color
    },
    secondary: {
      main: '#FF7F3E', // Customize the secondary color
    },
  },
});

export default theme;
